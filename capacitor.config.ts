import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.localhost.test',
  appName: 'Vue Memories Test',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
